[project]
name = "pypitravel"
version = "0.1.0"
description = "圆周旅迹行程规划导出与可视化工具"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
]
requires-python = ">=3.12"

[project.scripts]
pypitravel = "pypitravel.cli:main"
