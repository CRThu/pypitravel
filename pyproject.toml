[project]
name = "pypitravel"
version = "0.1.0"
description = "旅行规划与数据可视化工具"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
]
requires-python = ">=3.12"

[project.scripts]
pypitravel = "pypitravel.cli:main"
